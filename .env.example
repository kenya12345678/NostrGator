# NostrGator Environment Configuration Example
# ============================================
# Copy this file to .env and customize with your values

# SECURITY WARNING: This file contains EXAMPLE values only
# NEVER put your private key (nsec) in any configuration file!
# Keep your nsec secure in a password manager or hardware wallet

# Your Nostr Public Key (for whitelisting)
# Replace with your actual npub key from your Nostr client
NOSTR_PUBKEY=npub1example1234567890abcdefghijklmnopqrstuvwxyz1234567890abcdef

# Relay Configuration
RELAY_NAME="NostrGator Relay Suite"
RELAY_DESCRIPTION="Personal Nostr relay infrastructure"
RELAY_CONTACT="admin@yourdomain.com"

# Network Configuration - Core Relays
GENERAL_PORT=7001
DM_PORT=7002
MEDIA_PORT=7003
SOCIAL_PORT=7004
CACHE_PORT=7005

# Network Configuration - Extended Infrastructure
FILES_PORT=7006
LONGFORM_PORT=7007
LIVE_PORT=7008
MARKETPLACE_PORT=7009
GAMES_PORT=7010
BRIDGE_PORT=7011

# Monitoring and Management Ports
HEALTH_MONITOR_PORT=3001
CONTENT_DISCOVERY_PORT=7080
SECURITY_MONITOR_PORT=7081
NIP05_PORT=3005
PROMETHEUS_PORT=9090
EVENT_MIRROR_PORT=9091
NIP05_METRICS_PORT=9092

# Performance Settings
MAX_EVENT_SIZE=1048576  # 1MB for media uploads
MAX_CONNECTIONS=100
EVENT_RETENTION_DAYS=365

# Security Settings
ENABLE_WHITELIST=true
REQUIRE_AUTH=false  # Set to true for additional security if needed

# Logging
LOG_LEVEL=info

# Database Settings
DB_ENGINE=sqlite
VACUUM_FREQUENCY=24h

# Supernode Federation Configuration
ENABLE_SUPERNODE_FED=true
SUPERNODE_TRUST_THRESHOLD=3
FEDERATION_HEARTBEAT_INTERVAL=60
TOR_PROXY_ENABLED=true

# Tor Proxy Configuration
TOR_SOCKS_PORT=9050

# IMPORTANT SECURITY REMINDERS:
# 1. Your private key (nsec): NEVER put this in configuration files
#    - Store securely in password manager or hardware wallet
#    - Only use in Nostr clients for signing events
# 2. This .env file should not be shared or committed to version control
# 3. Only the public key (npub) above is used for relay whitelisting
# 4. Backup this configuration securely
# 5. Change default passwords and keys before production use

# Example npub format: npub1234567890abcdefghijklmnopqrstuvwxyz1234567890abcdef
# Example nsec format: nsec1234567890abcdefghijklmnopqrstuvwxyz1234567890abcdef
# Get your keys from any Nostr client like Damus, Amethyst, or iris.to
